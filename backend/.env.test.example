# 跑測試時用這個 DB（跟開發資料分開），複製成 .env.test 並填入自己的密碼
# 跑測試前要先建好這個 DB：
#   psql -U postgres -c "CREATE DATABASE bug_tracker_test;"
#   npm run test:setup   （把 schema 套到測試 DB）

DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/bug_tracker_test?schema=public"
